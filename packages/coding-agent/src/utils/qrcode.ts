/**
 * QR Code generator — moved to @musepi/collab-proto (pure, zero-dependency,
 * shared with the guest/web). This file re-exports for API compatibility;
 * new code should import from @musepi/collab-proto directly.
 */
export * from "@musepi/collab-proto/qrcode";
