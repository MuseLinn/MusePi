{
	"name": "@musepi/core",
	"version": "0.1.5",
	"description": "MusePi core — pure agent orchestration logic (goal/plan/permission/hooks/skills/swarm/task/tui/ask/todo/plugin), zero host imports",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json",
		"test": "node --test --test-reporter=dot --test-reporter-destination=stdout test/*.test.ts",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"files": [
		"dist/**/*"
	],
	"license": "MIT",
	"exports": {
		".": "./dist/index.js",
		"./*": "./dist/*"
	}
}
