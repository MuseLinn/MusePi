# @musepi/typescript-edit-benchmark

Edit benchmark suite using TypeScript source code mutations — evaluates how
well agents perform targeted code edits (fixtures + per-model results).

## Contents

- `fixtures.tar.gz` — benchmark fixture corpus
- `all_models_results.json` — recorded results across models

## Usage

```sh
bun run check      # biome + typecheck
```
