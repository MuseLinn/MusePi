# Dependencies
node_modules
.npm/

# Build output
dist/
packages/*/dist/
packages/*/dist-chrome/
packages/*/dist-firefox/
target/
*.tsbuildinfo
*.node
.source-hash
.bun-build
*[0-9a-f]*.bun-build
*.b64.js
/result
/result-*

# Environment
.env

# Editors
.vscode/
.zed/
.idea/
*.swp
*.swo
*.kate-swp
*~

# OS
*.tmp
.DS_Store

# Logs & profiling
*.log
tui-debug.log
*.cpuprofile

# Test & coverage
coverage/
.nyc_output/
packages/ai/test/.temp-images/

# Project specific
!.omp/
.omp/plugins/
.pi_config/
.opencode/
.worktrees/
.worktree/
compaction-results/
changes/
__pycache__/

# Scratch files
syntax.jsonl
out.jsonl
out.html
pi-*.html
.close_issue.sh

# Generated files
# NOTE: packages/coding-agent/src/export/html/tool-views.generated.js is intentionally
# TRACKED despite being generated — src/export/html/index.ts imports it at runtime
# (`with { type: "file" }`) and docs/porting-from-pi-mono.md requires keeping it in repo.
# Do not gitignore it; regenerate with `bun run gen:tool-views` after touching guest-client tool-render.
packages/natives/npm/
packages/coding-agent/src/utils/mupdf-wasm.wasm
/runs/
python/omp-rpc/src/omp_rpc.egg-info/
# parallel-agent worktrees
.wt/
CPU*.md
packages/coding-agent/binaries/

# robomp runtime state
python/robomp/data/
python/robomp/.cache/
python/robomp/src/robomp/static/
python/robomp/web/dist/
python/robomp/web/verify-out/
/verify-out/
python/robomp/.env

# Local, machine-specific boot perf baseline (see packages/coding-agent/scripts/bench-guard.ts)
packages/coding-agent/bench/boot-baseline.json

# Bazel
/bazel-bin
/bazel-out
/bazel-testlogs
/bazel-pi
/bazel-oh-my-pi
/bazel-musepi-omp
/.bazelrc.user
# Build output — but the NSIS include is a SOURCE file that electron-builder
# resolves from this path (packages/desktop-app/package.json "include"), so it
# must stay tracked. The pattern is `release/*` plus a negation rather than
# `release/`: git never descends into an excluded directory, so a negation
# could not re-include a file inside one. (This file was silently dropped from
# the index during a history rebuild and broke the Windows release job.)
packages/desktop-app/release/*
!packages/desktop-app/release/ensure-shortcuts.nsh
packages/desktop-app/dist/

# macOS haptic helper (compiled from electron/haptic-helper.m by build:haptic)
packages/desktop-app/electron/haptic-helper
musepi-session-*.html

# Compiled daemon binary for packaged GUI (built on Windows; do not commit)
packages/desktop-app/vendor/
.v2c/
.zcode/

# Local scratch notes (reference-repo triage etc.) — never committed
/local/

# i18n split scratch (split(1) output from a one-off migration)
.tmp-i18n-rem-part-*

# WorkBuddy agent session state (memory, outputs) — never committed
.workbuddy/

# Dev desktop data isolation (dev-desktop.mjs): isolated daemon home, socket
# dir and Electron userData for `bun run desktop:dev` — never committed.
packages/desktop-app/.desktop-build/
# The renderer bundle actually emitted by the dev/build pipeline lands one
# level UP from the package (`packages/.desktop-build/`), so the rule above
# never matched it and the directory showed up as untracked on every run.
packages/.desktop-build/

# Brand glyph intermediates from scripts/regen-brand-icons.py (manually pasted
# into tray.cjs) — never committed
.tray-glyph-*.b64
