# ZCode 改进吸收待办（对照 ZCode 更新日志筛选）

来源：用户提供的 ZCode 更新日志（2026-09）。每项标注状态与涉及面；"不适用"的已说明理由，避免重复评估。

## 待实施（有价值）

- [ ] **闲时任务**：任意会话用自然语言创建闲时任务，聊天中出现可直达编辑表单的卡片，与定时任务页对齐。（工作量中等，价值高；后端可复用 cron RPC，前端做创建卡片 + 跳编辑）
- [ ] **插件 @ 引用支持中文搜索**：中文正文或标点紧邻 @ 也能唤起面板；@ 面板候选匹配需要核对是否按 ASCII 边界切词。（小改动；定位 `packages/desktop-app/src/components/Composer.tsx` 的 @ 触发与候选过滤）
- [ ] **权限反馈与自定义回答支持换行输入；超五行内部滚动**：审批卡（AskCard / approval bridge）的文本输入改多行 + max-height 内滚。（小改动）
- [ ] **输入框「+」菜单扩展**：附件、目标（goal）、已启用插件、当前工作区文件与会话入口。（中等；Composer 已有 + 菜单骨架）
- [ ] **修复子智能体可选模型与所选供应商不一致**：审计 `task` 工具的模型解析是否与所选供应商的模型目录对齐（同类 bug 在任务卡/角色模型都有先例）。
- [ ] **Windows 截图链路加固**：窗口关闭到托盘后整幅截图失败（截图前先激活对应标签页）、后台截图残留缩放设置、跨进程恢复 3s 超时、标签页重建空白页、准备阶段错误短暂重试。（computer-use 工具链，涉及面较广）
- [ ] **应用缩放与 Windows 高 DPI 下浏览器点击坐标/截图偏移**：与上一条同链路。
- [ ] **降低工作区拖动指示线视觉重量**：小改动（拖动指示线样式）。
- [ ] **会话分享附件校验**：发布前校验、超限拦截（若分享功能支持附件时对齐）。

## 暂不适用 / 已有（勿重复评估）

- **Markdown 预览选中正文加入对话**：MusePi 无独立 Markdown 预览面板。
- **登录失败取消按钮/渠道列表、Start Plan、BigModel 购买页、飞书机器人、Repo Wiki、access_token 401**：ZCode 特有账号/商业体系。
- **工作区面板拆分（会话/终端/Side Pane 独立面板）**：大改版，需先出设计再排期。
- **统一「问题上报」文案**：MusePi 入口文案已按自身品牌命名。
- **PDF 文件读取恢复、旧 Coding Plan 历史迁移**：MusePi 未引入对应缺陷。
- **Chromium 结束页面进程导致主进程崩溃**：需确认 MusePi 是否有同类 `<webview>` 崩溃监听缺口（低优先级核查项）。
