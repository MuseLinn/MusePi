# MusePi 0.5.0 研发规划：参考吸收与插件化补齐

> **状态：小袁总已三审（2026-09-21）。拍板记录：① cordis 定案 v3——长期主义"用"、渐进收编，且**上游跟踪约束已解除**（`UPSTREAM.md` 2026-08-17 归档，MusePi 为独立上游；2026-09-21 小袁总确认 master 分支可删、基本自有设计）——试点成功即启动收编，不等 0.6.0 周期（§4.3）；② design 创作面对标 open-design 六面全量吸收（§4.4/M3）；③ Office 可编辑交付物确认做（M5，spike 先行）；④ 拖动指示线 #8 经 2026-09-21 复核确认未做，两处都改（M2-2.8）。硬性纪律：所有界面改动必须先有设计稿评审（§6 头部"设计稿先行"）。**
> 本文所有"现状"断言均带已核实的代码锚点（核对日期 2026-09-21），参考仓库分析基于本地 checkout 实证。
> 研发完成后本文中转入长期文档的部分应迁移至 `gui-design.md` / `gui-implementation.md` / `extensions.md` 等，本文降级为 release notes 素材。
> 配套工作文档：`zcode-absorption-todos.md`（ZCode 逐项吸收清单，9 项均未落地，是 0.5.0 第一批的输入）。

## 0.  TL;DR

- **ZCode**（2026-09-21 开源，Apache 2.0）的最大价值不在 UI 代码而在**会话消息的语义模型**：v4「行协议 + 只读投影 store + 轮渲染单元」三件套，直接对应我们"会话消息的显示和加载渲染"短板。吸收语义，不重写 wire 协议。
- **cordis 定案 v3（长期主义：用，渐进收编，约束已解除）**：cordis（MIT，核心仅 117KB/9 文件）是 Koishi 生态的组合内核，dsh 全底座跑在它上面（1729 文件 import）。MusePi 自 2026-08-17 起已是**独立上游**（不再跟踪 oh-my-pi），cordis 化的最大障碍不复存在：0.5.0 试点（vendor + 两个 spike），**试点成功即启动 strangler-fig 收编**，不绑定 0.6.0 周期。理由与阶段划分见 §4.3。
- **open-design 0.23.1** 给 design 模式真正的产品形态：**六个创作面**（Prototype / Live Artifact / Deck / Template / Media / Other）+ **agent-era 插件规范**（SKILL.md + `open-design.json` sidecar，插件不进 UI 运行时）。已定案：**六面结构全量吸收，站在巨人肩膀上再做自己的改动**，不做"语义等价"式的缩水重实现（§4.4/M3）。
- **workdsh v0.1.0-alpha.7**（dsh 插件，WorkBuddy 风格）给三个我们没打磨好的对象提供了**已验证的对象模型**：专家（引用技能对象+修订）、连接器（独立 MCP 实例 + 每会话选择 + 可见 chip）、Office 可编辑交付物（工作副本 + Tiptap 链路）。照抄对象模型，不抄实现（它绑死 dsh cordis）。
- 0.5.0 分六个里程碑（§6），验收标准随项给出；预估主力批次为 M1（消息渲染）+ M3（design 模式）+ M4（专家/连接器）。**所有界面改动设计稿先行**。

---

## 1. 现状盘点（musepi-omp @ 0.4.36，全部锚点已核实）

### 1.1 模式 / 预设：语义层齐全，但"四个模式没区别"属实

- 内置四预设定义在 `packages/coding-agent/src/presets/resolve.ts:260-328`（`BUILTIN_MODE_TEMPLATES`）：
  - `work`：空预设（完整工具集、无 prompt 覆盖）；
  - `chat`：`promptComplete` + 对话 persona + `modelRole: fast` + `compaction.enabled: false` + `extensions: []`（`resolve.ts:266-282`）；
  - `design`：5 段 prompt（role/workflow/brief/artifact/boundary，`resolve.ts:283-315`），**不设工具白名单是刻意的**（注释 `:287`）；
  - `creator`：1 段创作 persona（`resolve.ts:316-327`）。
- 继承/`promptComplete`/settings 覆盖的解析机制完整（`resolve.ts:134-216`），daemon RPC `modes.list/get/save` 齐备（`daemon/server.ts:5057-5129`），GUI 取名链 `resolveModeLabel` 完备（`desktop-app/src/lib/mode-label.ts`）。
- **缺口（用户反馈属实）**：四模式的差异**只在 prompt/settings 层**。design 模式没有对应的创作面 UI——没有 open-design 式的创建入口（简报/平台/保真度/模板选择）、没有产物面板的 mode 感知、没有创作过程中的方向选择器。prompt 里的 `artifact.manifest.json` 契约（`resolve.ts:305-308`）在 FilePane 侧没有对应的"产物识别"接线（FilePane 按扩展名渲染，不读 manifest）。⇒ 模式语义已接线，**模式的"产品面"没接线**。

### 1.2 插件化：注册表机制存在，但"自己的组件在插件管理里不显示"属实

- 内置扩展注册表 `extensibility/extensions-center/builtin-registry.ts:30-49` 的 `BUILTIN_EXTENSIONS` **只有 2 项**（`task-card-swarm` 样式、`shell` 桌面壳）。
- 大量"已经是插件化形态"的内部能力**没有登记**：bundled skills（`src/bundled-skills/`）、tool-render 卡片工具（guest-client `tool-render/`）、magic keywords、内置 hooks、modes 主题包等。它们在扩展中心的数据源里以 `native` provider 出现，`ExtensionsCenter.tsx:418` 的注释自承 "for native items so builtins stay reachable (TUI tabs skip native)"——**TUI 的 provider tab 直接跳过 native**，GUI 也只有 all tab 能看到，且无统一"内置"徽章叙事。
- 与 dsh 对齐的语义只做了"只读+可禁用"（builtin-registry.ts 头注释），没做 dsh 的 **patch 覆盖层 / bundle / 安装状态机**（§4.2）。⇒ 0.5.0 要做的是"**登记补全 + 展示分层**"，不是重写扩展系统。

### 1.3 能力中心 / 技能市场：骨架在，交互与绑定有出入（用户反馈属实）

- `desktop-app/src/components/CapabilityCenter.tsx`（1076 行）已有：来源/排序下拉、级别筛选 chips、网格/列表切换、详情抽屉（SKILL.md 渲染、ignore 开关、用户文件删除）、获取页（Git URL 安装 + `MarketplaceGrid`）、诊断 tab（扩展健康报告）。
- `SkillMarketView.tsx` / `UnifiedPluginsView.tsx` 与扩展中心并存，**两个市场视图的数据源和入口没有统一**（CapabilityCenter 走 `skills.list`，插件 tab 走 `plugins.list` / extensions registry）。
- 与 workdsh 的技能页差距：没有"修订"概念（技能不可版本化/回滚）、没有"专家引用技能"的对象关系、批量管理缺少绑定确认。⇒ M4 打磨。

### 1.4 侧板文件预览/编辑：预览已 rich，编辑是"单文件内联"，缺工作副本模型

- `desktop-app/src/components/FilePane.tsx` 已支持：Markdown 渲染/源码切换、HTML live iframe（openchamber 对齐）、tree-sitter 高亮文本、图片、PDF（pdf.js 内联）、DOCX（docx-preview）、右键菜单。
- 缺口（对照 workdsh）：**没有 Office 可编辑工作副本**（workdsh 的 Tiptap 文档链路 + `content_*` 工具 + 修订保留）；**没有 Library**（跨会话资料库、全文检索、选中修订送入新会话）；**没有项目-会话绑定**（workdsh 的 PROJECT-CONVERSATION-HANDOFF）。

### 1.5 会话消息显示/加载渲染：功能全，语义模型落后 ZCode 一代

- 现状：guest-client `Transcript.tsx`（含 `custom_message` 渲染分支 `:613-760`）+ tool-render registry（`guest-client/src/tool-render/registry.ts:42-97`）按工具名映射渲染器；加载态是组件内状态，没有"何时不该显示 loading"的集中语义。
- 对照 ZCode v4（§3.1）：我们没有 turn 分组渲染单元、没有"权限确认/AskUserQuestion/compact 期间抑制 loading"的集中规则、没有滚动锚定/行高缓存、没有只读投影层的 snapshot/delta 一致性纪律。⇒ M1。

### 1.6 TUI 与移动端

- TUI 成熟（agent-hub `docs/agent-hub.md`、marketplace TUI、hooks、skills 扫描），0.5.0 以跟随维护为主。
- 移动端：`packages/mobile`（Capacitor Android 壳）+ guest-client 移动入口，`docs/mobile-design.md` 已定稿（2026-08-24）并完成一轮交互修复（2026-08-31，6 项）。定位"随身遥控器"。0.5.0 跟随 M1 的渲染语义受益（移动入口复用 guest-client 渲染）。

### 1.7 ZCode 吸收清单状态

`docs/zcode-absorption-todos.md` 2026-09-15 审计：**9 项均未落地**。2026-09-21 复核 #8（拖动指示线）确认**仍未做**：`CustomGroups.tsx:232,250` 还是两组 1.5px inset 重环（`gui-settings.css:647,2622`），`SessionSidebar.tsx:993-997` 侧栏项目重排**依旧没有任何指示样式**（全仓无 `gui-drop-line` 类）。⇒ #8 两处都改（M2-2.8），"指哪一处"的疑问就此消解——不是二选一，是两件都要做。其余 8 项全部并入 0.5.0 批次（§6）。

---

## 2. 参考仓库更新状态（本次已执行）

| 仓库 | 本地路径 | 更新后版本 | 说明 |
|---|---|---|---|
| deepseek-harness | `deepseek-harness/` | 0.1.6-alpha.2（`ddefc45fbc`） | 快进 3833 提交；残留 42 个 `.agents/notes` 删除为本地既有的瘦身保留 |
| open-design | `open-design/` | 0.23.1（`894d55466b`） | 快进 153 提交，工作区干净 |
| ZCode | `zcode/` | 初开源于 2026-09-21（`872ad96`） | 新增 reference checkout，Apache 2.0，blob:none 浅克隆 |
| workdsh | `workdsh/` | v0.1.0-alpha.7（`de5077b`） | 新增 reference checkout，浅克隆；与 dsh 0.1.6-alpha.2 Web Profile 对齐 |

---

## 3. 参考仓库吸收分析

### 3.1 ZCode（zcode/）—— 会话消息语义

ZCode 是"AI 编程工作台"（Electron 桌面 + Web + CLI 一体，pnpm/Node 24）。与本仓关系：同为 agent 客户端，**消息渲染语义可直接吸收**；底座不同（它的协议是 zcode-protocol-v4，我们是 collab-proto），吸收**语义而非协议**。

**核心三件套**（`zcode/packages/ui/src/v4/`）：

1. **行协议 + 只读投影 store**（`conversationProjectionStore.ts`）
   - 会话 = `ConversationRow[]`（kind: `assistantText` / `reasoning` / `toolCall` / `subagent` / `userInput` / `turnHeader` / `timelineMarker` / `hookInvocation`），唯一写入方是订阅推送，UI 只读。
   - 三条纪律：snapshot 整体替换绝不 merge；delta 帧仅在 `frame.fromSeq === snapshot.seq` 衔接时 apply，**断档不猜不缓存**，携水位重订阅由服务端裁决 resume/snapshot；optimistic overlay 只用于 pending 命令展示，不产生会话事实。
   - 附带语义：runtime 回收（换代）时的有界退避重订（250/1000/3000ms）、ACK 后 2s 权威投影宽限期。
2. **轮渲染单元**（`conversationTurnRenderUnits.ts`）
   - `ConversationTurnRenderUnit`：turnId 聚合 —— header、可见 user inputs、assistant work rows、**workSegments**（原始输入与每条 accepted guide 各成一个独立视觉工作段）、flowItems（同轮内 user/assistant 可见交错顺序）、tail rows、hookInvocations（不进折叠）、latestAssistantTextRow（fork/retry/preview 只挂这段）。
   - 折叠边界读 workSegments 而非平铺行列表；browser 自动轮尾截图等"轮尾特殊行"有一等位置。
3. **加载可见性规则**（`chatLoadingVisibility.ts`）
   - 权限确认、AskUserQuestion 在等待用户时**禁止显示 loading**（弹窗/问答卡已是唯一进度反馈）；`compact` / `goalVerifier` 活跃时同样抑制。这是我们完全没有的集中语义。
   - 配套：`timelineScrollAnchor.ts` + `timelineRowHeightCache.ts`（滚动锚定/行高缓存）、`conversationTimelineLiveTail.ts`（live tail）、`SessionDataLayer`（租赁式会话数据层）。

**吸收策略（M1）**：把三件套语义映射到 guest-client Transcript 层——
- (a) 在 transcript 归一化层引入"轮渲染单元"中间表示（不改 wire 协议，entries → render units 的纯函数，可测）；
- (b) 抽出集中式 loading 可见性判定（对齐现有 ApprovalCard/AskCard/compact 事件）；
- (c) 滚动锚定 + 行高缓存补进 Transcript；
- (d) 长会话加载引入"窗口化渲染/水位重订阅"纪律（我们 daemon 已有 entries 分页，缺客户端断档纪律）。
- 代码可直接借鉴（Apache 2.0），注意保留许可证头。ZCode 其余吸收项走 `zcode-absorption-todos.md`。

### 3.2 deepseek-harness（deepseek-harness/）—— 插件化语义与 cordis 参照系

dsh 已全部 cordis 化：每个包是 cordis 插件，以 `ctx.*` 服务组合（`packages/` 下 60+ 包，1729 个文件 import cordis）。注意 **dsh 不是"双核"——cordis 是它的唯一组合核**，dsh 的一切（含 agent 运行时）都是 cordis Context 上的插件。对我们，dsh 的价值是**"插件化该长什么样"的完整参照系**：对象模型、状态机、每会话组合都能对标；框架本身是否采用见 §4.3 定案。

**值得吸收的对象与语义**：

1. **preset = 每会话 agent 组合**（`packages/preset/`）：一个预设 = 一个目录 + `agent.cordis.yml`，会话按预设组合 tools/prompt sections/skills，`agent-presets` 负责发现/挂载/复制式创作，`persona` 是可组合的行（换身份不只换工具）。核心技术手段是 **cordis 的 Context fork**——每个 agent 会话一个 fork 出的作用域，preset 的插件只挂进那个作用域。⇒ 我们的 mode 已有 extends/promptComplete/settings 覆盖（`resolve.ts`），**缺的正是"按会话组合 agent"的挂载执行面**——0.5.0 把 mode 从"prompt 覆盖"升级为"会话组合"（工具白名单、扩展集、persona、模型角色一并切换）。
2. **plugin-manager**（`packages/boot/plugin-manager/`）：profile bundles（`dsh.profile.bundles` 有序列表 = 配置优先级）、`cordis.patch.yml` 增量覆盖层（toggle 只写 disabled）、**安装状态机**（inspect → installing → cancelling → applying，requestId 可取消，失败恢复 package.json+lockfile，pnpm 11 构建脚本批准流）。⇒ 我们的 marketplace 安装是"clone+symlink+lock"（`docs/marketplace.md`），**缺安装状态机与失败恢复叙事**，M2 吸收。
3. **hmr**（`packages/boot/hmr/`）：插件源码与 profile 配置变更共享一个重载队列（模块替换 / Include 刷新 / 管理写操作），`ctx.hmr` 事件外露。⇒ 我们扩展的热重载目前是"手动重启生效"，M2 远期项对齐。
4. **extensions 组**（`packages/extensions/`）：`tool-cordis`（运行时发现工具）、`cordis-host-runner`/`cordis-client-runner`（host 半沙箱生命周期 + browser 半动态求值，`ctx.cordisInspect` 检视注册表）、`ui-cordis`（生命周期卡片）。⇒ "agent 自举地检视/装配插件"的元能力，M2-2.9 cordis 试点的直接对标物。
5. **agent-team**（`packages/experimental/agent-team/`，dsh 官方）：TeamMemberSnapshot（identity/mailbox/任务/共享检出）。workdsh 的"专家团"交互（§3.4）正是架在它上面。⇒ 我们已有 swarm/agent-hub（TUI），M4 对齐对象模型。

### 3.3 open-design（open-design/）—— design 模式的产品形态（六面全量吸收）

open-design 是本地优先设计工作台（Next.js web + daemon + ACP agent 运行时），0.23.1。与本仓关系：**design 模式与插件市场的产品形态基准**；已定案**直接吸收其六面结构与流程，在其上做自己的改动**，不做缩水的"语义等价"重实现。

**吸收点**：

1. **六个创作面**（`docs/modes.md`）：New Project 六 tab——Prototype / Live Artifact / Deck / Template / Media(Image/Video/Audio) / Other。两轴taxonomy：UI tab = 用户启动的工作流；skill mode（prototype/deck/template/design-system/image/video/audio）= daemon 索引路由的语义（`apps/daemon/src/skills.ts` 解析归一）。**全量吸收**：六 tab 的创建面板、project metadata（kind/intent/fidelity/platform/design-system）、模板目录（`/api/design-templates` 对标的 design-templates 根）、media 子面（Image/Video/Audio 的 provider/比例/时长/声线控制）。我们的差异化改动在其上叠加（中文工作流、我们的 token 体系、与 mode 预设的融合），不删减其结构。
2. **agent-era 插件规范**（`docs/plugins-spec.md`）：插件 = 可移植 `SKILL.md` + `open-design.json` sidecar（市场卡、预览、输入字段、上下文 chip）。**插件不进 UI 运行时**、无 postMessage/RPC 生命周期、"Use plugin" = 水合 brief+chips+资产后走普通 agent run。⇒ 与我们能力中心的对照：我们走 Claude 插件注册表兼容（`docs/marketplace.md`），open-design 走"SKILL.md 为锚、sidecar 纯增量"——**两种兼容可并存**（我们的插件带 SKILL.md 即自动获得 open-design 等价物），M4 统一市场视图时吸收其卡面/预览/use-case 查询字段。
3. **组件基元**（`packages/components/src/`）：button/dialog/form-controls primitives（CSS modules）。**吸收方式**：六面创建面板直接参考/复用其交互结构（表单控件形态、对话框节奏），视觉映射到我们的 `gui-design.md` token 体系（oklch 深紫 + 玻璃阶梯），不引入第二套视觉语言。
4. **产物契约**：Normal Artifact = entry file + artifact manifest（kind/renderer/exports）——与我们 design 模式 prompt 里的契约同构，说明该契约方向正确；0.5.0 把契约从"prompt 约定"升级为"daemon 校验 + FilePane 识别"的真实功能。

### 3.4 workdsh（workdsh/）—— 专家/连接器/可编辑交付物

workdsh 是 dsh 0.1.6-alpha.2 Web Profile 之上的插件组合（`dsh plugin` 生命周期，非第二运行时），WorkBuddy 风格。与本仓关系：**对象模型基准**（实现绑死 dsh cordis，不搬实现）。

**三层模型**（`docs/ARCHITECTURE.md`）：功能插件 = 可执行模块；bundle = 安装组合层；**专家/技能/连接实例/应用 = 业务对象**。一个功能插件管理多对象，提供方插件贡献预置对象。

**三个对象模型（直接吸收）**：

1. **专家（Experts）**：专家引用**技能对象 + 修订**（"专家任务：解析对象修订与依赖 → 选择已授权连接实例 → 通过原生专业组合创建会话 → 注入受控输入 → 工具执行 → 登记并验证成果"）。专家不是常驻 agent，是"身份 + 受控输入 + 已审修订"的会话组合。⇒ 我们 M4 专家团的基础。
2. **连接器（Connectors）**：独立 MCP 实例，凭据进**官方 credential service**，**按会话选择**、连接器名称 chip 常驻提示词旁、任务内只有该连接器的 MCP namespace。⇒ 我们的 MCP 配置是全局 `mcp.json`（`docs/mcp-config.md`），缺"每会话选择 + 可见 chip + namespace 隔离"，M4 吸收。
3. **Office 可编辑交付物**：工作副本模型（编辑不碰原件，修订保留），Tiptap 文档链路 + `content_*` 工具族，PPTX/XLSX/DOCX/HTML 各自格式范围声明（`docs/COMPATIBILITY.md`）。⇒ M5（小袁总确认做）。
4. **附加**：Library（本地资料库：Markdown/HTML/PDF/Word/PPT 入库、全文检索、选中修订直接送入新会话）；团队活动条（官方 Agent Team 面板 + Siri 式状态条共存）；`docs/UI-DESIGN.md` 中性灰黑紧凑规范（对照用，不替换 `gui-design.md`）。

---

## 4. 关键决策

### 4.1 会话消息渲染：吸收 ZCode 语义，不改 wire 协议

guest-client 的 collab-proto 不动；在 transcript 归一化层与 Transcript 组件之间插入"轮渲染单元"中间表示 + 集中 loading 可见性 + 滚动锚定。理由：wire 协议改动牵动 daemon/TUI/mobile 三端，0.5.0 窗口不值得；语义层收益 80% 在客户端。

### 4.2 插件化：补登记与展示分层，吸收 dsh 插件语义，0.5.0 不换组合底座

- **登记补全**：把 bundled skills、tool-render 工具、magic keywords、内置 hooks、主题包等登记进 `BUILTIN_EXTENSIONS`（`builtin-registry.ts:30` 加行即可，机制已备），全部带"内置·只读·可禁用"徽章；native provider 进 provider tab（去掉 TUI/GUI 的 skip-native 特例）。
- **语义对齐**：marketplace 安装补 dsh 式状态机（inspect/installing/cancelling/applying + 失败恢复 + 构建脚本批准）；扩展 enable/disable 改"patch 覆盖层"语义（只写增量，不物理改插件）。
- **能力缝**：对齐 dsh capability seams 文档纪律——每个内置能力声明自己的接缝（输入/输出/生命周期），供 agent 检视。
- 以上各项**框架无关**：无论 §4.3 的 cordis 收编走到哪一步，登记/展示/状态机都是 UI 与注册表层语义，照做不浪费；收编后它们自然映射（注册表→bundle 清单、patch 层→cordis.patch.yml、状态机→plugin-manager 等价物）。

### 4.3 cordis：定案 v3（2026-09-21 三审）——长期主义"用"，渐进收编，**收编约束已解除**

**事实**：cordis（`vendor/cordis`，`@deepseek-ai/cordis` 4.0.2，MIT，核心 src 仅 117KB/9 文件）是 Koishi 生态的 TS 插件/DI 框架（Context/Service/注入/作用域 fork/生命周期/配置驱动加载/HMR）。dsh 里 1729 个文件 import 它——**dsh 不是"双核"，cordis 是它的唯一组合核**，dsh 的一切（含 agent 运行时）都是 cordis Context 上的插件。小袁总指出的两点都成立：**cordis 与自有运行时不互斥**（它只是个库，局部共存没有障碍）；**"服务话语也是可以改的"**（一次性迁移成本，非长期约束）。

**关键背景修正（三审确认）**：MusePi 自 **2026-08-17 起已是独立上游**（`UPSTREAM.md` 归档：oh-my-pi 等一律视为参考吸收来源，无版本对齐义务；包名已改 `@musepi/pi-*`，版本线独立 0.4.x）。小袁总 2026-09-21 确认：master 分支可删、当前基本为自有设计。**这意味着 cordis 收编在 v2 中的决定性障碍（fork 同步约束）已不存在。**

**为什么长期判断是"用"**：

1. **0.5.0 路线图自己在逐项重新发明 cordis**：mode→每会话组合（dsh 用 Context fork 实现 preset 挂载）、patch 覆盖层、bundle、动态插件运行时（host 半沙箱+inspect）、安装状态机、HMR——这些加起来正是 cordis 的 Loader/patch/scope/HMR 能力集。逐项自研 = 长期维护一组比 cordis 更窄更弱的等价物。
2. **daemon 确实未必最优**：`server.ts` 已 10,336 行、巨型 RPC switch；单进程多会话 + 会话级组合（mode 挂载、连接器隔离、专家组合）天然是**作用域 DI** 的形状，正是 cordis Context fork 的强项。
3. **体量与许可成本可忽略**：MIT、117KB，dsh 已验证 vendor 路线可行。

**为什么仍然"渐进"而不是 0.5.0 直接换底座**（剩下的唯一理由）：

- **有状态 daemon 的大爆炸风险与上游无关**：`DaemonSessionHost` 是会话生命周期编排器（历史快照、再激活、WebSocket 协议），整体重写 = 高回归风险，strangler-fig 是唯一负责任的路径。
- **过渡期双话语成本**：需用收编边界框住（见下）。

**三阶段定案（v3：收编不再绑定 0.6.0 周期）**：

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| **0.5.0 试点**（M2-2.9） | vendor `@deepseek-ai/cordis` 到 `musepi-omp/vendor/cordis`；两个 spike：①动态插件运行时原型（对标 dsh `cordis-host-runner`：host 半沙箱 + inspect 注册表，挂载我们一种真实扩展）；②mode→每会话挂载（一个会话 fork Context、按 preset 组合工具/prompt/扩展的最小闭环） | 试点报告：加载/隔离/生命周期/检视四能力实测 + spike ②证明会话作用域组合可行 |
| **收编决策（试点成功后立即）** | 依试点报告决策：通过则立刻启动 strangler-fig，**不等到 0.6.0**；不通过则维持双轨（试点面保留 cordis，主干不变），损失仅 vendor 117KB | 决策记录（ADR） |
| **收编执行（若通过）** | daemon 宿主 cordis 化：先包 daemon 服务为 cordis 服务（session manager 第一个），再迁扩展装载为 Loader，WS 协议全程不动；每阶段可回滚 | 分阶段验收 |

**收编边界（代码卫生约束，不再是同步硬约束）**：cordis 依赖只进 daemon 宿主层与新增包，核心领域包（agent/ai/wire/sdk 等）保持零 cordis 依赖——这是清晰分层的纪律，便于回滚与测试，而非上游同步所迫。

### 4.4 design 模式：从 prompt 预设升级为创作面——六面全量吸收 open-design（已定案）

M3 给 design 模式接 open-design 式创作面，**六面全量对标：Prototype / Live Artifact / Deck / Template / Media(Image/Video/Audio) / Other**——直接吸收其 tab 结构、project metadata（kind/intent/fidelity/platform/design-system）、skill-mode 路由（daemon 侧解析归一）、design-templates 模板目录，再叠加我们的改动（中文工作流、`gui-design.md` token 视觉、与四预设的融合）。**不做"语义等价"的缩水重实现**；吸收中出现与我们底座冲突处（如它的 ACP 运行时），改的是适配层而非删减功能。

daemon 侧把 `artifact.manifest.json` 从 prompt 约定升级为校验+索引（FilePane/预览面板按 manifest 识别产物）。

### 4.5 专家团与连接器：按 workdsh 对象模型自建

专家 = 身份 + 技能修订引用 + 受控输入模板；连接器 = 每会话选择的 MCP 实例 + 凭据托管 + 可见 chip。架在我们现有 swarm/agent-hub 与 MCP runtime 上，不引入 dsh agent-team 的实现。

---

## 5. 不做的事（0.5.0 明确排除）

- 不重写 wire 协议；**0.5.0 不换组合底座**（cordis 以 vendor 试点形式引入；收编于试点成功后启动，§4.3）。
- 不做 ZCode 特有账号/商业体系（登录、Coding Plan、购买页——`zcode-absorption-todos.md` 已列不适用）。
- 不整体搬运 open-design / workdsh 的视觉皮肤（保持 `gui-design.md` 语言；吸收的是结构、对象模型与流程，视觉一律重映射到我们 token 体系）。
- 不做 iOS（移动端维持 Android 遥控器定位）。
- 不"修" coding-agent 的 48 个 pre-existing 测试失败（TTSR/transcript 渲染基线，与 0.5.0 无关，`zcode-absorption-todos.md` 工作纪律已载）。

---

## 6. 里程碑与批次

> 每项附验收。批次内可并行；M1→M3→M4 为主力链。所有 GUI 工作遵守 `AGENTS.md` "GUI Development Rules"、biome 门禁、`bun run check:ts` 全绿（对应包）、i18n 按域拆分（en `as const satisfies Record<ZhKey, string>`）。
>
> **设计稿先行（硬纪律，小袁总 2026-09-21 要求）**：凡涉及界面/交互改变的条目（M1 的折叠与 loading 表现、M3 全部、M4 全部、M5 全部），**开工前必须先产出设计稿**——新面板的按 `docs/mobile-design.md` 规格写独立设计规范文档（信息架构 / 组件规格 / 交互契约 / 视觉 token 映射 / 验收），既有面板的出改动章节并入 `gui-design.md`；设计稿经小袁总评审通过后才开始编码，**禁止边做边改**。各里程碑的首项即设计稿交付项。

### M1 会话消息渲染语义（ZCode 吸收）——第一批

| # | 项 | 来源 | 验收 |
|---|---|---|---|
| 1.1 | Transcript 引入"轮渲染单元"中间表示（entries → render units 纯函数层；turn 分组/折叠段/轮尾行/flow 交错） | ZCode v3.1 | 既有渲染快照测试不回归；新层单测覆盖分组/折叠/交错 |
| 1.2 | 集中 loading 可见性（权限确认/AskUserQuestion/compact/goalVerifier 期间抑制聊天 loading） | ZCode v3.1 | 审批等待时无 loading 闪烁；规则有单测 |
| 1.3 | 滚动锚定 + 行高缓存 | ZCode v3.1 | 流式追加不跳动；长会话滚动 60fps 基线（devtools 实测记录） |
| 1.4 | 客户端断档纪律（分页水位记录、断档显式重订阅、不猜测 merge） | ZCode v3.1 | 模拟丢帧测试：不重放不丢序 |
| 1.5 | @ 引用中文搜索触发（token 边界正则，排除 email） | zcode-todos #2 | 该文档验收条 |
| 1.6 | 问答卡换行输入（desktop AskCard 4 处 input→textarea，与 guest 对齐） | zcode-todos #3 | 该文档验收条 |
| 1.7 | 子智能体模型下拉供应商一致性审计修复 | zcode-todos #5 | 该文档验收条 |
| 1.8 | **设计稿**：轮渲染单元的折叠边界/loading 表现/轮尾行的视觉与动效改动章节（并入 `gui-design.md`），评审通过后再动 1.1-1.4 的视觉部分 | 设计稿纪律 | 小袁总评审通过 |
| 1.9 | **desktop-app ChatView 移植**（小袁总 2026-09-21 增设）：同一套轮渲染语义落到 desktop 渲染器（turn header / 工作段折叠 / 轮尾·hook 行 / loading 收敛 / 审批等待进度线），视觉走 desktop 自己的液态玻璃 token，不照搬 guest-client CSS；改动章节并入 `gui-design.md`，评审后动 | 小袁总指示 | desktop 侧渲染快照/单测不回归；截图验证折叠态/展开态/等待态三视图 |
| 1.10 | **全产品液态玻璃统一设计稿**（小袁总 2026-09-21 增设，视觉总纲）：guest-client 及全部既有面板的液态玻璃视觉与交互统一（动效丰富流畅、一镜到底），产出 token 映射与动效语言规范，评审后按面板分批收敛 | 小袁总指示 | 设计稿（信息架构/组件规格/动效契约/token 映射）评审通过；各面板收敛有逐面板验收 |
| 1.11 | **全量窗口化渲染（虚拟化）调研与试点**（小袁总 2026-09-21 痛点立项）：分页的三个实测代价——① TurnRail 侧边导航无法显示全量轮次（老轮次未加载即无行可测位）② 上翻加载慢（分页取数延迟）③ 会话恢复慢（逐块加载）；方向：全量 entries 内存化 + 视口窗口化渲染（行高测量/前插补偿/锚定复用 M1.3 状态机），先 mem-bench 实测基线再定窗口化粒度 | 小袁总指示 | 实测报告（分页 vs 窗口化 恢复/滚动/内存对比）；试点面板滚动 60fps；TurnRail 全量轮次可导航 |

### M2 插件化基础组件 + 闲时任务 + cordis 试点——第一、二批

| # | 项 | 来源 | 验收 |
|---|---|---|---|
| 2.1 | `BUILTIN_EXTENSIONS` 登记补全（bundled skills/tool-render/magic keywords/hooks/主题包）+ native 进 provider tab + "内置"徽章 | §1.2 | 全部内部插件化组件在插件管理可见、可禁用、只读保护生效 |
| 2.2 | marketplace 安装状态机（inspect/install/cancel/失败恢复/构建脚本批准） | dsh plugin-manager | 断网/取消/脚本拦截三场景各有明确状态与恢复路径 |
| 2.3 | enable/disable 改 patch 覆盖层语义（增量 disabled，不物理改插件） | dsh | toggle 后插件文件不动；覆盖层可 diff |
| 2.4 | 能力缝声明纪律（内置能力接缝文档 + 检视入口） | dsh capability seams | 新增内置能力必须带接缝声明（CI 检查或评审 checklist） |
| 2.5 | 闲时任务自然语言创建 + 聊天内卡片（schedule_task 工具 + tool-render 卡片 + 跳转任务中心） | zcode-todos #1 | 该文档验收条 |
| 2.6 | + 菜单扩展（已启用插件/工作区文件） | zcode-todos #4 | 该文档验收条 |
| 2.7 | 会话分享附件校验（统计/显式拦截/剥离可见） | zcode-todos #9 | 该文档验收条 |
| 2.8 | 拖动指示线统一（**2026-09-21 复核确认两处都未做**：分组 1.5px inset 环减重 `gui-settings.css:647,2622`；侧栏项目重排 `SessionSidebar.tsx:993-997` 补轻量插入线；抽共用 `.gui-drop-line`） | zcode-todos #8 | 拖动反馈清晰不刺眼；尊重 `gui-motion-off`/`prefers-reduced-motion`；与其他 dnd 拖拽视觉语言一致 |
| 2.9 | **cordis 试点**（§4.3 阶段一）：vendor cordis；spike ①动态插件运行时原型（host 半沙箱 + inspect，挂载一种真实扩展）；spike ②mode→每会话挂载（Context fork 按 preset 组合工具/prompt/扩展最小闭环） | dsh cordis-runner + preset | 试点报告：加载/隔离/生命周期/检视四能力实测 + spike ②会话作用域组合跑通 |
| 2.10 | **客户端设置面板接线审计（小袁总 2026-09-21 增设）**：设置项全量盘点（已接线/未接线/受单进程 daemon 限制三档），面板适度增删合——未接线项先标注或隐藏，daemon 能扩展的逐步接；改动章节并入 `gui-design.md` 评审后动 | 小袁总指示 | 设置项三档清单落档；面板无死按钮；每处增删有记录 |

### M3 design 模式创作面——第二批（主打，六面全量）

| # | 项 | 来源 | 验收 |
|---|---|---|---|
| 3.1 | **设计稿**：创作面设计规范文档（六 tab 信息架构、project metadata 字段、表单控件规格、token 映射、动效；对齐 `mobile-design.md` 文档规格） | open-design modes + 设计稿纪律 | 小袁总评审通过后才动 3.2-3.6 |
| 3.2 | 六面创作面板全量实现：Prototype / Live Artifact / Deck / Template / Media(Image/Video/Audio) / Other（创建入口、tab 元数据、模板目录、media 子面控件） | open-design `NewProjectPanel.tsx`/`docs/modes.md` 结构吸收 | 六 tab 可创建对应 kind 的设计会话；metadata 落盘且 daemon 可读 |
| 3.3 | skill-mode 路由：daemon 侧解析归一（prototype/deck/template/design-system/image/video/audio），替换选中模板为项目主 skill | open-design `apps/daemon/src/skills.ts` 语义 | 选模板创建的项目走模板 skill；未选走默认 skill |
| 3.4 | `artifact.manifest.json` 升级为 daemon 校验 + 索引 + FilePane/预览面板按 manifest 识别产物 | open-design artifact 契约 + 我们 prompt 既有契约 | 无 manifest 产物给出提示；有 manifest 产物在侧板出现预览卡 |
| 3.5 | 设计系统轴：design-templates 模板库 + design-system 引用（token 集选择进 metadata） | open-design | 创作面板可选设计系统；产物 manifest 记录引用 |
| 3.6 | 模式差异化收尾：chat 模式的 UI 简化 + creator 模式创作辅助入口 | §1.1 | 四模式在 UI 层面可感知区分 |

### M4 专家团 / 连接器 / 能力中心——第三、四批

| # | 项 | 来源 | 验收 |
|---|---|---|---|
| 4.1 | **设计稿**：专家/连接器/能力中心三个面的设计规范文档（对象卡片、chip、选择器、绑定可视化；token 映射） | workdsh UI + open-design 卡面字段 + 设计稿纪律 | 小袁总评审通过 |
| 4.2 | 专家对象模型（身份 + 技能修订引用 + 受控输入模板 + 会话组合创建；若 2.9 spike ②成立，会话组合走 cordis 挂载，否则走现有 session 组合） | workdsh 专家 | 创建专家→以专家身份起会话→修订随专家升级可选择 |
| 4.3 | 连接器（每会话 MCP 实例选择 + 凭据托管 + 名称 chip + namespace 隔离） | workdsh 连接器 | 同一会话只暴露所选连接器工具；凭据不出 credential store |
| 4.4 | 能力中心统一视图（技能/插件/市场单一数据模型 + open-design 式卡面字段：预览/use-case 查询/sidecar；专家↔技能↔连接器绑定可视化） | §1.3 + open-design plugins-spec | 单一入口；卡面含预览与 use-case；绑定关系可视化 |

### M5 侧板文件预览/编辑打磨——第四批（Office 已确认做）

| # | 项 | 来源 | 验收 |
|---|---|---|---|
| 5.1 | **设计稿**：FilePane 编辑态 / Library / Office 工作副本三个面的设计规范文档 | workdsh UI-DESIGN 结构 + 设计稿纪律 | 小袁总评审通过 |
| 5.2 | **spike（≤3 天）**：Office 编辑链路技术验证（Tiptap docx/pptx/xlsx 路线选型与最小原型） | workdsh Office | spike 报告：选定路线 + 格式范围初表，评审后定 5.3 范围 |
| 5.3 | Office 工作副本编辑（选定路线实现；编辑不碰原件，修订保留；格式范围声明照 workdsh COMPATIBILITY 纪律） | workdsh Office | 编辑不碰原件；修订列表可回退；范围外格式明确提示 |
| 5.4 | Library 最小版（跨会话资料库 + 全文检索 + 选中内容送入新会话） | workdsh Library | 入库/检索/注入三链路通 |
| 5.5 | 高 DPI/应用缩放坐标与截图加固 | zcode-todos #6/#7 | 该文档验收条（#6 拆 2-3 PR） |

### M6 TUI / 移动端收尾——贯穿

- TUI：native provider tab 补内置（随 2.1）、marketplace TUI 跟随 2.2 状态机。
- 移动端：复用 M1 渲染语义（移动入口自动受益）；新增本会话连接器 chip 与专家选择的移动适配（设计稿随 4.1 补移动章节）；`docs/mobile-design.md` 补两节。
- Windows 截图链路加固的跨进程部分（zcode-todos #6）与桌面打包回归。

---

## 7. 风险与开放问题

1. ~~cordis 决策~~ → **已定案 v3：长期"用"、渐进收编；上游约束已解除，试点成功即收编**（§4.3）。
2. ~~zcode-todos #8 指向~~ → **已消解：两处都未做、两处都改**（§1.7，M2-2.8）。
3. ~~design 创作面范围~~ → **已定案：六面全量吸收**（§4.4）；剩余不确定性是 Live Artifact 的"connector-backed 数据产物"与 M4 连接器的耦合排期，M3 设计稿阶段定。
4. **cordis 收编的回归风险**（v3 改写）：上游约束既已不存在，收编的真实风险 = 有状态 daemon 的 strangler-fig 执行质量。缓解：分阶段验收、每阶段可回滚、核心领域包零 cordis 依赖的分层纪律（§4.3 收编边界）。
5. **Office 编辑**（5.2/5.3）：小袁总确认做，但编辑链路（Tiptap 或等价）是全新依赖，spike 结论可能压缩格式范围（先 DOCX/HTML 后 PPTX/XLSX 是大概率路径），以 spike 报告评审为准。
6. **设计稿评审节奏**（新）：设计稿先行意味着 M3/M4/M5 各有评审卡点，需小袁总预留评审时间；评审不通过则里程碑顺延，不绕过。
7. **0.5.0 研发窗口**：大改并行（M1 渲染 + M2 插件 + M3 创作面）期间建议分支化研发、里程碑间合并，避免长期偏离 main。

## 8. 文档同步清单（随研发推进勾选）

- [x] 本文（`docs/0.5.0-roadmap.md`）—— 三审拍板已并入（2026-09-21 第三轮：独立上游确认，cordis 收编约束解除）
- [x] `harness-engineering/AGENTS.md`：reference 表补 zcode/workdsh 行，更新 dsh（0.1.6-alpha.2）与 open-design（0.23.1）版本；Relationship 段更新为"独立上游"（本轮已同步）
- [ ] M2-2.9 试点启动时：cordis 收编边界写成 ADR（核心领域包零 cordis 依赖的分层纪律）
- [ ] M1 设计稿（1.8）：轮渲染单元/loading 可见性改动章节并入 `gui-design.md` 后开工
- [ ] M1 完成后：轮渲染单元/loading 可见性语义并入 `gui-implementation.md`；`zcode-absorption-todos.md` 关闭已落地项（#2/#3/#5）
- [ ] M2 完成后：`extensions.md` / `marketplace.md` 补 patch 层、状态机、内置注册表章节；`zcode-absorption-todos.md` 关闭 #1/#4/#8/#9；**cordis 试点报告归档 `docs/archive/`**
- [ ] M3 设计稿（3.1）：新建 `docs/design-mode-spec.md`（六面创作面设计规范）
- [ ] M3 完成后：创作面与产物契约并入 `gui-design.md` / `gui-implementation.md`；`docs/archive/modes-plan.md` 链接更新
- [ ] M4 设计稿（4.1）：新建 `docs/experts-connectors-spec.md`
- [ ] M4 完成后：新增 `experts.md` / `connectors.md`（或并入 `agent-hub.md` / `mcp-config.md`）；能力中心交互并入 `gui-implementation.md`
- [ ] M5 设计稿（5.1）：新建 `docs/filepane-library-spec.md`；spike 报告（5.2）归档 `docs/archive/`
- [ ] M5 完成后：FilePane/Library 章节 + Office 格式范围声明文档
- [ ] 0.5.0 发布：`CHANGELOG.musepi.md` + 版本号（0.4.36 → 0.5.0）+ 本文归档至 `docs/archive/`

## 9. 进度记录

- **2026-09-21 · M1.2-1.3 视觉落地完成并截图验证**：TurnHeader（hairline·圆点·第N轮·模型·时间·状态+耗时）、工作段折叠/展开、tail/hook 行折叠豁免全部接入 `Transcript.tsx`；per-turn model 跟踪（turn 内最后一个 model_change，含轮内切换）；新增 `row-kinds.ts` 分类器（复用 round-collapse `isTurnStart`）；i18n 中英双档。测试：render-units 新增 model 跟踪 4 例 + 新建 round-collapse 折叠豁免 4 例，guest-client 全量 **517 pass / 0 fail / 57 files**，biome + tsgo 全绿。实装验证：`verify-live.py`（mock-host + dev server + headless Chromium）截图确认折叠态/展开态渲染正确，产物 `docs/review/0.5.0-m1-transcript-design/shots/live/`，设计文档已加"实装验证"小节。待续：**M1.3 滚动锚定**（timelineScrollAnchor 语义移植，现有 followKey/lockRef 机制在 Transcript.tsx ~1282 行）；D 流式进行态 live 截图待真实流式会话补充。
- **2026-09-21 · M1.2 loading 可见性收敛落地**：① client 双传输（guest + host）新增 `compacting` 快照状态（auto_compaction_start/end 事件对跟踪）；② Composer 工作 pill 改接 `shouldShowChatLoading`（approvalPending=host 审批 / askPending=`uiRequest` 非空 / compacting），抑制态保留安静 ghost 停止键（不丢 abort 可达性，无矩阵动画避免双重反馈）；③ ApprovalCard 加"等待你的决定"进度线（accent shuttle，`--spring-liquid` 1.6s，token 化）；④ i18n 中英双档。决策记录：4 项设计决策点按设计稿默认执行（turn header 保留模型名/段摘要规则拼接/token 行默认展示/hook 行单独展示）。biome + tsgo + 48 测试全绿。待续：Transcript 渲染层接入 turn header + 工作段折叠（大改，窗口化列表与 RoundFold 交织，需独立工作块）；滚动锚定。

- **2026-09-21 · M1.8 完成（设计稿已评审通过）**：`docs/review/0.5.0-m1-transcript-design.md` + 高保真 mockup（`docs/review/0.5.0-m1-transcript-design/mockup.html`，Playwright 截图 `shots/`）。四个视觉态：A 轮工作段折叠/展开、B pending 期间聊天 loading 抑制（卡片承担进度反馈）、C 轮尾行与 hook 行（不进折叠）、D 流式进行态。token 全部取自 `guest-client tokens.css` 暗色段，未发明新 token；小袁总批复"全面液态玻璃风格、切实可行"。待办：评审决策点 4 项（turn header 模型名/段摘要文案来源/token 行开关/hook 行聚合）随 1.2-1.4 视觉实现前敲定；改动章节后续并入 `gui-design.md`。
- **2026-09-21 · M1.1 纯函数层落地（不涉视觉，先行）**：`packages/client-core/src/components/transcript/render-units.ts` + 单测 19/19。`buildTurnRenderUnits`（turn 分组/回复锚点 strict 语义/工作行/轮尾行/hook 行分桶，复用 round-collapse 的 `isTurnStart`）、`classifyTranscriptRow`、`hasPendingAsk`（ask toolCall 无匹配 toolResult = 待答，结构性判定）、`shouldShowChatLoading`（§B 判定表纯组合）。调查结论：审批仅 host 侧（guest transport 恒 null）、ask 是普通工具、compact 是 start/end 事件对；**hooks 引擎在 coding-agent 自有**（`extensibility/hooks/`，TUI 有 hook 呈现），hook 消息持久化为 custom_message 但 customType 是 hook 作者自定义自由串（如 `file-trigger`），分类器按项目既有 `hook:` 命名空间约定（与 hook capability 的 `hook:<type>:<tool>:<name>` extension id 一致）匹配，前缀强制列入 M2 能力缝纪律。biome + tsgo 全绿。下一步：1.2-1.4 视觉实现（渲染层接入 + ApprovalCard 进度线 + 滚动锚定，设计稿已批）。
- **2026-09-21 · M1.9 desktop 落地（架构事实修正 + model 接线）**：调查发现 desktop-app 直接复用 guest-client 的 React `Transcript` 组件（非自有渲染器），M1 turn header/折叠/tail 豁免**天然全量继承，无需移植**；desktop 侧实际只补 `model` prop 接线（`snap.state.model` → provider/id，ChatView.tsx，tsgo + biome 全绿）。审批等待反馈 design 裁决：desktop 保持 pulse beam + paused-for-you wave 等价物，不照搬 guest 进度线。改动章节并入 `gui-design.md` §5q。同时增设 **M1.10 全产品液态玻璃统一设计稿**（视觉总纲：动效丰富流畅、一镜到底；guest tokens.css 暗色段 + desktop gui.css 液态玻璃段双基准映射）。剩余：M1.3 滚动锚定（共享组件一处落地两面生效）；M1.10 设计稿；desktop 三视图人工走查。
- **2026-09-21 · 共享组件归位：`guest-client` → `client-core`（包改名 + 目录迁移）**：M1.9 调查确认 desktop-app 深度消费（137 文件）guest-client 的共享渲染层（Transcript/i18n/widgets/tool-render），"guest" 语义已名不副实。执行：`packages/guest-client` → `packages/client-core`，包名 `@musepi/client-core`；desktop-app/coding-agent/scripts/docs 全部 import 与路径引用迁移（含 tsconfig paths、root 脚本 --cwd、verify-live.py、brand-accent 测试内仓库相对路径）；README 重写定位。验证：client-core biome + tsgo 全绿、**517/517 测试通过**；desktop-app tsgo 0（biome 仅剩 FloatingScrollbar 一条预存基线告警）；coding-agent tsgo 0。历史文档（gui-design.md 等）中的旧名随各章节下次改动时自然更新，不单独清扫。
- **2026-09-21 · M1.3 滚动锚定落地（M1 语义闭环）**：移植 ZCode `timelineScrollAnchor` 状态机（`client-core/src/components/transcript/scroll-anchor.ts` 纯函数层 + 23 例单测）：following=用户滚动权（仅真实用户输入可翻转）；程序化贴底/布局 scroll 只记几何账；commit 前 reconcile 用户意图（wheel 上滚领先 scroll 事件一帧，不被流式 commit 吞掉）；内容增长动作 stick/hold（释放后绝不拉回）。Transcript 接线：scroll 监听按 programmatic/user 分类、wheel/touch/keydown 意图捕获、follow effect 先 reconcile 再贴底、ResizeObserver 跟随期尾部重钉（覆盖流式 delta/图片/高亮撑高，rAF 合并）。未移植：virtualizer 测高补偿（无虚拟化，native overflow-anchor 兜底）与「回到底部」按钮（`shouldShowBackToBottom` 已导出，UI 待设计稿评审）。验证：biome + tsgo 全绿，**540/540 测试**（517+23 新），mock-host 回归冒烟通过。60fps 长会话基线留待 mem-bench 实测记录。
- **2026-09-21 · M1.10 设计稿首版（评审稿）+ M1.11 立项**：`docs/review/0.5.0-m1.10-liquid-glass.md`——双 token 词汇盘点（client-core `--glass-*`/`--spring-liquid` vs desktop `--gui-motion-*`，单向映射不合并）；玻璃四层配方 + L0-L3 层级表；动效时长阶梯；一镜到底五条原则（共享元素 morph、整屏淡出禁用，desktop morphFrame 收编先例）；win32 blur 缺席的 sheen+rim 补偿成规范；批次 A-D 收敛计划。四个评审决策点待小袁总批复。M1.11 虚拟化立项依据（小袁总实测痛点）：TurnRail 无法显示全量轮次 / 上翻加载慢 / 会话恢复慢——全量 entries 内存化 + 视口窗口化，先 mem-bench 实测基线再定粒度。
- **2026-09-21 · M1.10 批次 A 首件落地：回到底部按钮**：按 L2 float 规格（ghost 圆钮、accent rim、glass-strong 底 + blur、`--spring-liquid` 入场、reduced-motion 降级）；sticky 尾部定位（`.tr-root` 末子元素，滚离时 sticky 约束钉在面板底）；M1.3 状态机语义驱动显隐（`shouldShowBackToBottom(followingUi, entries.length)`，following 已状态化）；点击平滑滚回 + `smoothScrollRef` 裁决（动画中间帧不计为用户上滚，落底 re-arm）。实机冒烟：上滚出现 → 点击回尾隐藏（verify-back-bottom.py，shots/live/back-bottom-*.png）。biome+tsgo 全绿、540/540。
- **2026-09-21 深夜 · 重复设计清扫（小袁总截图问责：回底按钮有俩）**：① 根因查实并修复——desktop ChatView 在共享 Transcript（M1 落地，自带 ZCode parity `.tr-back-bottom`）之上又叠了本地 `JumpToBottomButton.tsx`，删除本地件与配套 CSS，唯一入口归共享组件（`ffb4bf915`）；② 重名组件扫描（desktop × client-core 8 件）：BlurText/ShinyText 完全相同、SpotlightCard core 版更优（pointer 事件覆盖触屏）、DotMatrixMark 桌面版是超集（reduced-motion + 动效开关停 rAF、空闲节流）——四件收敛归位 client-core，桌面删本地副本统一导入（`07e8236b4`）；AccentToggle（桌面图标式 vs core 触点圆点+i18n）/ApprovalCard（桌面更厚）/Composer（桌面 2307 行 vs core 1200）/ContextRing（桌面带用量类型导出）判定**有意分化保留**；WorkStatusOrb（顶栏会话活动 vs Transcript 轮内 thinking）与 toast 双管道（桌面事件式 vs core notices 队列，数据源不同）**非重复**。会话滚动语义确认已是 ZCode parity（`scroll-anchor.ts` 状态机 + 回底 re-follow）。
- **2026-09-21 深夜 · M2.8 拖动指示线落地**：新增共享 `.gui-drop-line`（2px accent 条 + 柔光，spring-snappy 140ms 入场，`gui-motion-off`/reduced-motion 降级为静态）；侧栏项目头拖拽上半/下半显示插入线、落点 before/after 精确插入（块体保留 swap 语义，`SessionSidebar.tsx`）；设置分组 dragover 环减重（45%→28%、头环全强度→35%）。tsgo 全绿（`0238fdba2`）。
